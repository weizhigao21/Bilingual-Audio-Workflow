# 变更记录

基于 PyQt6 的三步式配音工作流工具（字幕提取 → 语音合成 → 音频混音）。

## [v2.0.1] - 2026-08-11

### 修复
- 进度条"第一个任务完成直接 100%"根治（两处根因）：
  - 任务完成后 GUI 刷新面板把 DONE 状态进度条强制置 100%，覆盖任务级累计进度 → 批量执行中进度条不再由面板刷新重置，完全由累计进度信号驱动
  - 多任务批量时把 TTS 片段总数误用作进度条上限，百分比值被 clamp 成 100% → 多任务（批量/组）不再转发片段总数，统一按 0-100 累计百分比显示
- 流水线/批量中混音"等待音频时进度条一直 0%"：流水线同步执行改为转发任务内进度（映射到任务累计区间），提取音轨、加载配音、声道检测、合成、导出阶段进度实时可见
- 执行前源文件检查：每个任务开始执行前检查源文件是否存在；不存在时自动尝试"双语-"前缀路径变体修复（应对源文件夹被程序重命名/用户改名），仍找不到则跳过任务并明确提示——避免语音/混音白跑一遍后才发现源文件失效

### 变更
- 批量/组执行启动时重置三个步骤进度条，避免残留上次运行的值
- 单步执行（手动点步骤按钮）前也检查源文件存在性，缺失时弹窗提示

## [v2.0.0] - 2026-08-09

### 新增
- **流水线批量执行模式**（默认）：字幕先全部完成，语音生成与混音交错并行——音频语音完成立即进混音队列，混音空闲即接下一个，同时后台持续生成下一个音频的语音，多音频批量总耗时大幅缩短
- 语音并行精细化控制：
  - **全局并发上限**（`edge_max_concurrent`，默认 8）：多个音频并行合成时的总请求数上限，防止触发微软 Edge TTS 限流
  - **流水线语音并行**（`pipeline_tts_workers`，默认 2）：同时进行语音生成的音频数，设 1 = 按音频逐个生成
- 混音并行真正生效：流水线模式的混音并行度改为跟随混音配置"线程数"，多任务可同时混音（日志显示"混音并行 N"）

### 修复
- 流水线模式跨线程信号回调失效：TTS 内部回调改用 DirectConnection，解决语音报"未执行"但实际已生成
- 流水线语音完成后任务列表不刷新进度（成功分支漏发完成信号）
- 流水线进度条左右横跳：改为任务级累计（每完成一个任务 +1/N，单调递增）
- 批量"按任务"/文件夹组执行时语音"第一个任务完成就 100%"：步骤2 多任务改为任务级累计
- 批量后部分文件夹未添加"双语-"前缀：批量重命名补上 STEP_SKIPPED 并支持失败自动重试（最多 3 次）

### 变更
- 批量/组执行三个步骤进度条统一为"已完成任务数 / 总任务数"累计递增显示
- TTS 配置面板新增"全局并发上限"、"流水线语音并行"两项配置

### 兼容性
- 旧配置文件无新字段时自动使用默认值，行为不变

## [v1.5.0] - 2026-08-09

### 新增
- 任务列表支持**文件夹分组**：拖入文件夹创建"文件夹任务组"节点，展开显示内部音频树（每个音频文件一个子任务）
- 组节点实时显示整体进度与完成数（如 `50% ▶ [文件夹] 纪录片 (1/2)`）
- 选中文件夹组后可对其内所有音频**逐个执行**指定步骤；右键组节点支持移除整组、重跑组内全部步骤
- 拖入的单个文件为散任务；选中组时拖入文件自动加入该组

### 修复
- 选择任务后三个步骤进度条残留上一次运行值的问题，现按字幕/语音/混音各自的完成状态显示（完成/跳过=100%、待处理/失败=0%、运行中保持实时进度）
- 批量/文件夹组混音时进度条"一个完成直接走完"的问题，现按"已完成混音数/总音频数"递增显示（任务内细节保留在累计区间内）

### 变更
- 文件夹导入行为：由"展开成多个独立任务"改为"创建文件夹分组节点"，保留文件夹结构
- 步骤1 单任务字幕提取改为按文件识别（不再把整个文件夹一次性传给 infer.exe）；批量模式仍按文件夹去重一次处理

### 兼容性
- 旧任务 task.json 无 `group_id` 字段，自动按散任务显示，行为不变

## [v1.4.0] - 2026-08-09

### 新增
- 混音配置新增"导出音频质量"配置：档位预设（低 128k/44.1kHz、标准 192k/44.1kHz、高 256k/44.1kHz、极致 320k/48kHz、自定义）+ 高级参数（比特率 128k~320k、采样率 44.1kHz/48kHz、声道 立体声/单声道）
- WAV 导出支持位深选择（16/24/32bit），MP3/AAC 忽略该选项
- 新增 API 服务器连通性测试函数 `test_api_server`

### 优化
- 导出参数从硬编码 192k/44.1kHz/2ch 提升为可配置项，覆盖纯音频导出（pydub/NVENC）与视频换音轨（replace_audio_in_video）两条路径
- 批量 API 请求增加异常兜底，服务器未返回音频时正确标记失败而非 results 中出现 None

### 兼容性
- 旧配置文件无新字段时自动使用默认值（192k/44.1kHz/立体声），行为不变

## [v1.3.0] - 2026-08-03

### 新增
- 步骤1 字幕提取支持文件夹模式：文件夹导入的任务直接传文件夹给 infer.exe，自动扫描其中全部音频，避免逐个文件处理；批量模式同样改为传去重后的文件夹列表，显著提升处理速度
- 文件夹模式下字幕查找仅使用精确文件名匹配，避免误关联同目录下其它文件的字幕

### 优化
- 打包体积：spec 排除误收集的无关依赖（Pythonwin、pythonnet、PIL、cryptography、pydantic 等）及未使用的 Qt DLL（Qt6Pdf/Qt6Network/Qt6Svg），打包体积由 93.3MB 降至 64.2MB

## [v1.2.2] - 2026-07-29

- 优化启动速度：将步骤模块（whisper/tts/mixer/audio_utils/batch_executor）改为延迟导入，避免启动时加载 numpy、pydub、edge-tts、requests 等重型库

## [v1.2.1] - 2026-07-29

- 优化启动速度：ffmpeg NVENC 检测从模块导入时改为懒加载，避免启动时阻塞等待 `ffmpeg -encoders`

## [v1.2.0]

### 新增
- TTS 配置面板新增缓存管理：显示缓存大小和复用统计，支持一键清除

### 修复
- Edge TTS 合成后临时 .mp3 文件泄漏（每片段浪费双份磁盘空间）
- `auto_volume` 动态音量模式因字符串不匹配完全失效的问题
- 全部流程完成后偶发闪退（重命名函数未处理跨盘符 ValueError）
- SQLite 连接漏关、全局缓存无限增长、任务状态不持久化等问题

### 优化
- 混音内存占用（64bit→32bit，消除中间副本，内存减半）
- 启动速度（合并冗余 subprocess 调用）
- 任务列表百分比移至文件名最前，方便按进度排序
- 清理死代码、配置残留，添加 .gitignore

## [v1.1.0]

### 新增
- 文件夹拖放导入（"+ 添加文件夹"按钮，自动递归扫描音频和字幕）
- 混音完成后自动为源文件夹添加"双语-"前缀（混音配置中可开关）
- 应用图标设置
- PyInstaller 打包配置

### 优化
- 任务结构简化：移除 `workspace/<timestamp>/` 中间目录，语音文件直接存于 `workspace/<字幕MD5>/`
- 添加任务时自动检测已生成的语音和混音文件，自动跳过已完成步骤
- 批量执行进度条修复，三个步骤均可实时显示进度
- TTS 缓存与日志路径统一到 `resources/` 目录
- 全局屏蔽 subprocess 控制台窗口弹出

### 修复
- 打包后 `sys` 未导入导致的启动崩溃
- Edge TTS 单条生成添加 15 秒超时，防止网络卡住导致进度永久阻塞
- Edge TTS 超时/异常后自动重试（最多 3 次，间隔 1 秒）
- `subprocess.Popen` 猴子补丁与 `asyncio` 冲突（改用类继承方式）
- 文件夹重命名时机（同文件夹所有任务完成后才重命名，避免混音中断）
- 重命名目标层级（重命名拖入的根文件夹而非音频文件父目录）
- 文件夹扫描不支持 `.wav`/`.wma` 格式的问题

## [v1.0.0]

- 初始版本：三步骤工作流整合
