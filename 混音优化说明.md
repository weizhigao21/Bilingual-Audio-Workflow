# 混音优化说明（2026-09-26）

本次基于工作区现有的包拆分和未提交修改增量优化，未改动字幕提取、TTS 合成或批量编排逻辑。更改需要从源码运行，尚未重新打包 dist 中的程序。

## 项目与处理链路

PyQt6 界面由 `src/workflow_gui/` 和 `src/widgets/` 组成，配置来自 `src/config.py`。任务依次经过字幕提取、TTS、混音。单任务和批量混音共用 `src/steps/step3_mixer.py::mix_single_task`，声道检测、RMS、叠加与导出位于 `src/steps/audio_utils/`。

混音按配音文件名中的时间戳排布片段，可根据原声位置安排配音声像，或通过能量起始点修正时间。它是双语叠加工具；目前没有人声分离，也没有把原语音替换成译文。

## 已落地的改进

| 问题 | 调整与实际影响 |
| --- | --- |
| 原软限幅在接近峰值时改变波形，可能增加谐波 | 默认使用整段统一增益的峰值保护，把超过 -1 dBFS 的样本峰值压到目标以下；左右声道同一增益，保留动态和声像。尖峰可能让整段明显变轻。旧软限幅可选。 |
| RMS 和最后限幅为整段音频创建多份大数组 | RMS、峰值扫描与量化按块计算，减少中间数组和内存带宽开销；不再为调试日志计算整段绝对值。 |
| 32bit 正峰值转回整数可能绕回负值 | 高位深源使用 float64 累加；量化前使用可精确表达整数上界的 float64，并舍入和限界。 |
| 动态音量先把左右波形相加，反相信号互相抵消 | 用各声道功率平均计算 RMS 包络，起始检测也按功率分析。 |
| 立体声配音只读取左声道 | 改为单声道下混，右声道独有的内容不再丢失；真正反相的立体声配音仍可能在下混时抵消。 |
| 起始裁切后，动态增益对应错误时间；整段被裁掉时仍可能混入 | 先裁切，再从实际落点计算增益包络；支持负时间戳，完全裁掉的片段跳过。 |
| 采样率转换使用快速插值，降采样可能产生混叠 | 新增默认开启的高质量重采样，使用 FFmpeg swr、64 点滤波；只在配音采样率不一致时额外启动 FFmpeg。同采样率直接复用。 |
| 视频被强制转为 44.1kHz/16bit，再按输出转换 | 提取时保留源采样率，解码为 32bit PCM；传给编码器时保留位深。 |
| CPU 导出 M4A/AAC 忽略所选码率，AAC 格式依赖隐式推断 | 统一 PCM 管道导出，显式指定码率、编码器与封装。支持 8/16/32bit 输入，无需中间 WAV。 |
| “GPU 加速”不对应当前音频编码路径 | 移除该界面开关；视频流复制、音频软件编码。旧配置字段保留，旧函数导入接口保留。 |
| 视频源选择 WAV/MP3 仍执行视频换轨流程 | 按目标格式分支：MP4 复制视频，其他格式仅导出音频。 |
| FFmpeg 管道可能阻塞，停止后留下不完整输出 | 独立线程运行 communicate，同时收发管道，主线程支持取消并回收子进程；先导出临时文件，成功后再替换正式输出。 |
| 混音异常静默回退，丢失音量/对齐等配置却显示成功 | 正式混音遇到错误明确失败；numpy 是必需依赖。 |
| 并发检测切换线程池容量时发生竞争 | 获取线程池与整批提交同锁，容量按配置设置，不随每个文件的片段数变化。 |
| 24bit WAV 整段先降到 24bit 再升回 32bit | 直接按块取 32bit PCM 的高三字节，保留经典 PCM WAV 头。选择 24/32bit WAV 时在混音前提升计算精度。 |
| 长音频整段解码和累加占用大量内存 | 默认达到 20 分钟后，将源 PCM、浮点累加缓冲及混音结果置于临时磁盘文件；片段按需解码，分块量化并编码。阈值可在混音配置中改为 0（始终启用）。 |

## 本机测量

基线是本次编辑前的工作区代码，包含用户此前未提交的优化，不能直接用 Git HEAD 代替该基线。Python 3.10、NumPy 2.2.6；各耗时取三次运行中位数。

输入：120 秒、44.1kHz、16bit 双声道合成音；24 个两秒配音片段，同采样率，无声道检测、起始/内容对齐、文件解码或编码。

| 音量模式与实现 | 混音耗时 | tracemalloc 峰值新增分配 |
| --- | ---: | ---: |
| fixed：原实现 | 0.2954 秒 | 202.2 MiB |
| fixed：新版旧软限幅 | 0.2517 秒 | 81.4 MiB |
| fixed：新版保真峰值保护 | 0.1187 秒 | 68.9 MiB |
| auto：原实现 | 0.4047 秒 | 204.9 MiB |
| auto：新版旧软限幅 | 0.3835 秒 | 84.1 MiB |
| auto：新版保真峰值保护 | 0.2426 秒 | 71.6 MiB |

fixed 新峰值模式约快 2.49 倍，峰值新增分配约少 66%。峰值模式与软限幅声音行为不同；若只比较旧软限幅模式，则 fixed 耗时约减少 15%，内存约减少 60%。这些数字不是整条工作流加速比，也不是进程总内存：输入在测量前已创建，FFmpeg 子进程及未被 tracemalloc 跟踪的分配不计入。

另一组输入：30 秒主音频、12 个 24kHz 配音片段转换到 44.1kHz。快速转换中位耗时 0.0214 秒；高质量转换 0.7382 秒。这里进程启动占比较高，几百或上千片段时会积累成明显等待，不能称为无成本升级。

降采样质量检查：将 48kHz 中的 18kHz 测试音转为 24kHz，并去除首尾 20ms。快速路径的混叠 RMS 为 -13.32 dBFS；高质量路径降至本项目 RMS 统计下限 -100 dBFS（至少改善约 86.7dB）。这是单一合成信号的抗混叠测试，不代表真实作品的主观音质评分。

上述测量是短音频内存路径，不能套用到所有视频素材。长音频磁盘路径减少整段音频常驻内存，但增加临时磁盘占用和读写；批量并行仍会叠加每项任务的内存与磁盘需求。

## 使用设置

- 质量优先：保真峰值保护、高质量重采样开启；母版可选 WAV 24bit。增加位深不会恢复源文件已经丢失的细节，但可减少后续混音计算的量化损失。
- 速度优先：关闭高质量重采样；保真峰值保护仍可保留。配音与原音同采样率时，质量开关不会为片段增加转换进程。
- 希望保留旧版较响的听感：选择软限幅，接受峰值波形被压缩的代价。
- fixed 匹配的是整段 RMS，auto 跟随短时 RMS，都不是 LUFS 标准化。“关闭”模式仍应用手动配音增益，界面名称已修正。
- 长视频或多任务内存吃紧时降低混音线程数。试听对比时取消“跳过已存在”或换导出目录，避免复用旧成品。
- 长音频磁盘缓冲默认从 20 分钟启用；可在混音配置中降低阈值，设为 0 则总是启用。单小时 48kHz 立体声素材的源 PCM、float64 累加缓冲和成品 PCM 理论上约需 5.5 GB 临时空间，内容对齐还会增加单声道缓冲。

## 验证和边界

运行 `python -m unittest discover -s tests -v`。测试覆盖 PCM 极值与 24bit 字节精度、峰值形状、RMS 反相、起始检测、负时间戳与裁切包络、右声道配音、线程池并发切换、抗混叠、同采样率免转换、码率参数、8/16/32bit 管道导出、MP3/M4A/AAC/MP4 封装、视频转音频、WAV 三档位深、stderr 排空、stdin 阻塞时取消，以及失败保留已有输出。另用 Qt offscreen 检查配置面板加载与保存。

后续值得单独做的工作：

1. 进一步减少磁盘缓冲和读写：当前使用整段磁盘映射累加，仍需足够的临时空间；按时间块调度配音可进一步降低磁盘占用，但需要处理跨块片段与内容对齐。
2. 基于语音活动区间的音量匹配，以及带平滑包络的原声避让。现有整体/短时 RMS 容易受背景音乐、静音长度影响。
3. 最终输出阶段的过采样真峰值检查、降位深抖动。当前 -1dB 是混音采样点的峰值余量，后续重采样与有损编码仍可能产生峰值变化。
4. 重采样批处理或常驻转换器：当前每个异采样率片段一个 FFmpeg 进程，质量模式的启动开销仍可优化。
5. 配音过长时的重叠处理和内容对齐算法：目前保留原时间轴及原长度，超出末尾的配音仍会截断，音乐场景下能量起点也不一定是人声起点。应结合真实素材评估，不能直接用自动加速或截断代替。

FFmpeg 参数依据：[官方重采样文档](https://ffmpeg.org/ffmpeg-resampler.html)。
