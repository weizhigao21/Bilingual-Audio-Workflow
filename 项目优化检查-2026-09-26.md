# 项目优化检查（2026-09-26）

本次检查基于当前工作区代码。这里已有未提交的模块拆分与混音优化；本轮在其上检查 TTS、缓存、任务导入、批量调度和状态持久化。旧版《项目体检报告》对应 v1.1.0，其中部分问题已在先前修改中修复，不能直接当成当前缺陷清单。

## 已修复并验证

| 影响 | 发现与改动 |
| --- | --- |
| 生成设置变化后仍复用旧声音 | Edge 缓存键曾只包含文本和声音，遗漏语速及音量；API 缓存键曾遗漏服务器来源。现在纳入这些参数。输出目录新增生成配置标识，导入任务及修改 TTS 设置时检查；不匹配会重新生成配音和重做混音。 |
| 片段失败但整项任务显示成功 | Edge/API 两种模式原先都可能把失败片段计入进度后发成功信号，导致混音缺失台词。现在失败、未处理、字幕读取失败或用户停止都会使语音任务失败。 |
| API 停止时可能卡住 | 原代码停止领取队列任务后仍调用 `Queue.join()`，未领取项无人 `task_done()`。现在先等待工作线程退出，再清理未领取项。 |
| 缓存与下载残片 | 缓存拷贝失败曾仍写 SQLite 记录；直接复制还可能让并发读取者拿到半成品。下载和缓存写入改为同目录临时文件后原子替换，空音频不作成功输出。 |
| 流水线执行未勾选步骤 | 只选步骤 2 时原实现仍把完成任务送到混音队列。现在只有勾选步骤 3 才启动混音线程、入队并更新其进度。 |
| 重复导入生成的混音 | 文件夹扫描递归进入源目录里的“双语/”输出目录。现在跳过该目录，避免成品成为下一轮输入。 |
| 状态识别与文件完整性 | AAC 成品现在可被识别；同秒导入同名文件的任务 ID 用微秒区分；配置和任务状态 JSON 采用临时文件加原子替换，写入失败保留上次文件。 |
| Edge 并发设置未传入 worker | 配置面板的全局并发上限现在进入 TTSBridgeWorker 构造的运行配置。 |

由于输出目录的生成配置标识是新加的，旧目录首次运行会重新生成配音。旧缓存文件暂时留在磁盘中，不会因本轮升级被删除。若配音设置已改变，重新混音会覆盖同名旧成品；需要保留旧版成品时，请先使用另一导出目录。


## 仍值得优先做

1. **长音频磁盘缓冲（本轮已实现）**：达到混音配置阈值（默认 20 分钟）时，源 PCM、累加数组和成品 PCM 使用临时磁盘文件，配音片段按需解码；混音峰值扫描与量化分块执行。此路径用更多磁盘读写换取更低内存占用，并不保证比短音频路径更快。临时空间依源时长、采样率和是否启用内容对齐增长，批量并行时叠加。见 [混音优化说明](混音优化说明.md)。
2. **任务恢复（本轮已实现）**：启动时读取工作区各任务的 `task.json`，恢复任务与文件夹分组；运行中断、输出缺失或 TTS 配置变化的步骤重置为待处理。任务移除时同时移除状态记录，避免下次启动又出现。无子任务的空分组没有单独状态文件，因此不会恢复。
3. **Edge 音频文件格式**：代码把 `.mp3` 临时文件复制到 `.wav` 文件名；当前解码路径可以按内容识别，但其他程序可能按扩展名判断格式。应明确转换为 PCM WAV，或把片段文件名和混音扫描改为实际格式。
4. **相同文本的并发合成**：多个线程可能同时查询同一未命中的缓存并分别发请求。缓存写入现已安全，但请求数仍可通过“同键请求共享一个进行中的结果”降低。
5. **API 停止响应**：未领取任务已能及时清理，但进行中的批量 HTTP 请求仍可能等到 300 秒超时。需要可取消的请求策略及与线程退出联动的超时设计。
6. **源根目录的成品识别**：当重命名后的“双语-*”目录本身作为源目录，成品可能与源文件同级；当前仅排除了“双语/”子目录，尚需明确区分同级的 `_mixed` 成品与用户原始文件。
7. **字幕与目录大批量性能**：文件夹导入时每个媒体都重新列出同目录文件寻找字幕；同目录文件很多时可缓存目录列表。此项优先级低于上面的正确性和内存问题。

以上 3–7 是后续建议。

## 验证范围

运行 `python -W error::ResourceWarning -m unittest discover -s tests -q`，共 44 项离线测试通过。新增测试覆盖任务重启恢复、长音频磁盘缓冲与普通路径的数值对比、MP3/WAV/视频导出、停止与失败清理。`compileall` 与 `git diff --check` 均通过。没有连接在线 Edge TTS 或外部 API，也没有重新打包 `dist` 中的程序。
