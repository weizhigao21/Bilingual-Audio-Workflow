# 双语音声工作流 v2.0.2

[![GitHub](https://img.shields.io/badge/GitHub-weizhigao21%2FBilingual--Audio--Workflow-blue)](https://github.com/weizhigao21/Bilingual-Audio-Workflow)

基于 PyQt6 的三步式配音工作流工具：**字幕提取 → 语音合成 → 音频混音**，一键批量处理，输出双语配音视频/音频。

## 功能概览

| 步骤 | 功能 | 说明 |
|---|---|---|
| 步骤1 | 字幕提取 | 调用 faster-whisper（infer.exe）从视频/音频中提取 LRC 字幕 |
| 步骤2 | 语音生成 | 使用 Edge TTS / API 将字幕文本合成为配音片段 |
| 步骤3 | 音频混音 | 将配音混入原始音频，支持声道定位、起拍对齐、内容对齐 |

## 运行环境

- Windows 10+
- Python 3.9+
- ffmpeg（需在 PATH 中）

## 安装

```bash
pip install PyQt6 pydub numpy edge-tts requests
```

## 首次启动

1. 运行 `python main.py`
2. 在弹出的配置对话框中指定 **faster-whisper 项目路径**（`infer.exe` 所在目录）和 **工作区目录**
3. 点击"确定"后进入主界面

## 使用流程

### 单个任务

1. 拖拽视频/音频文件到任务列表，或点击 **+ 添加视频/音频**
2. 程序自动识别同目录字幕文件并关联

### 文件夹分组导入

- 拖入**文件夹**（或点击 **+ 添加文件夹**）时，会创建一个"文件夹任务组"节点，展开显示文件夹内的**音频树**（每个音频文件一个子任务）
- 组节点实时显示整体进度与完成数，如 `50% ▶ [文件夹] 纪录片 (1/2)`
- 选中文件夹组后点步骤面板的"开始"，组内所有音频按顺序**逐个执行**（字幕 → 配音 → 混音）
- 右键组节点可**移除整个文件夹组**或**重跑组内全部步骤**；拖入的单个文件为散任务，选中组时拖文件会加入该组

### 跳过步骤

- **+ 添加字幕**：直接指定字幕文件，跳过步骤1
- **+ 添加配音**：直接指定配音目录（含 .wav 文件），跳过步骤2
- **自动检测**：添加任务时自动检测已存在的字幕、配音文件和双语输出，跳过对应完成步骤

### 批量执行

- 三种模式：
  - **流水线**（默认）：字幕先全部完成，然后**语音生成与混音交错并行**——一个音频的语音完成立即混音，同时继续生成下一个音频的语音，总耗时最短
  - **按任务**：每个任务依次执行全部步骤，再跑下一个任务
  - **按步骤**：所有任务先跑步骤1，再全部步骤2，再全部步骤3
- 步骤1 批量模式：一次加载模型处理所有文件
- 步骤3 并行模式：多线程同时混音多个任务（可在混音配置中设置线程数）
- 批量/文件夹组执行时，三个步骤进度条按"任务间累计 + 任务内片段进度"平滑混合显示（语音的片段级进度实时可见，不再"长时间不动后猛跳"）

## 配置面板

### 字幕配置（步骤1，通过 **字幕配置...** 按钮打开）

| 参数 | 说明 |
|---|---|
| 推理设备 | auto / cpu / cuda |
| 计算精度 | float32 / float16 / int8 |
| 字幕格式 | lrc / srt / vtt |
| VAD 阈值 | 语音活动检测灵敏度 |
| 最小静音/语音时长 | 控制段落切分粒度 |
| 段落合并 | 合并短片段，设置最大间隔和时长 |

### TTS 配置（步骤2，通过 **TTS 配置...** 按钮打开）

| 参数 | 说明 |
|---|---|
| TTS 模式 | Edge TTS（免费在线）/ API（自定义服务器） |
| 声音 | zh-CN-XiaoxiaoNeural 等 20+ 中文声音 |
| 语速 / 音量 | 百分比调节 |
| 线程数 | Edge 模式下单个音频内部的片段并发合成数（建议 5-8） |
| 全局并发上限 | 多个音频并行合成时的总请求数上限（默认 8），过高会触发微软服务限流 |
| 流水线语音并行 | 流水线模式下同时进行语音生成的音频数（默认 2，设 1=按音频逐个生成） |
| 批量 API | 单次请求发送多个文本，减少网络开销 |
| 缓存信息 | 显示当前缓存条数、总大小和累计复用次数 |
| 清除缓存 | 一键删除所有 TTS 音频缓存，释放磁盘空间 |

### 混音配置（步骤3，通过 **混音配置...** 按钮打开）

| 参数 | 说明 |
|---|---|
| 导出格式 | MP4（视频，H.264）/ MP3 / M4A（AAC）/ WAV（无损）/ AAC |
| 音频质量 | 档位预设：低（128k/44.1kHz）/ 标准（192k/44.1kHz）/ 高（256k/44.1kHz）/ 极致（320k/48kHz）/ 自定义 |
| 比特率 | 128k / 192k / 256k / 320k（自定义档位时可选） |
| 采样率 | 44100 Hz / 48000 Hz（自定义档位时可选） |
| 声道 | 立体声 / 单声道（自定义档位时可选） |
| WAV 位深 | 16 / 24 / 32 bit（仅 WAV 导出生效） |
| 音量 | 配音相对原音频的音量偏移（dB） |
| 自动音量 | fixed（匹配原音频 RMS 响度）/ auto（逐帧动态匹配）/ off（纯固定增益） |
| 声道检测 | 自动检测配音在原音频中的方位（左/中/右） |
| 起始对齐 | 跳过配音片段的静音起始部分，对齐到原音频起拍点 |
| 内容对齐 | 用原音频人声起始点纠正字幕时间戳误差 |
| GPU 加速 | 使用 NVENC 硬件编码导出 |
| 导出目录 | 自定义输出路径（默认输出到源文件目录下的 `双语/` 文件夹） |
| 线程数 | 批量混音时的并行任务数（流水线模式的混音并行度同样受此控制；注意并行混音内存叠加） |

## 打包发布

```bash
pip install pyinstaller
pyinstaller 双语音声工作流.spec
```

输出目录：`dist/双语音声工作流/`，内含 `双语音声工作流.exe` 及全部依赖文件，启动即开无需解压。

> 注意：`infer.exe`（faster-whisper）需要单独部署，不包含在打包中。

> 分发时将整个 `双语音声工作流/` 文件夹打包为 zip 即可。

## 目录结构

```
双语音声工作流/
├── main.py                     # 程序入口
├── 双语音声工作流.spec          # PyInstaller 打包配置
├── README.md
├── src/
│   ├── config.py               # 配置管理
│   ├── task_manager.py         # 任务模型与队列
│   ├── workflow_gui.py         # 主窗口
│   ├── widgets.py              # UI 组件
│   └── steps/
│       ├── step1_whisper.py    # 字幕提取（subprocess → infer.exe）
│       ├── step2_tts.py        # 语音生成桥接
│       ├── step3_mixer.py      # 音频混音
│       ├── batch_executor.py   # 批量执行编排
│       ├── tts_worker.py       # TTS 工作线程（Edge / API）
│       ├── tts_utils.py        # TTS 工具函数
│       ├── tts_cache.py        # SQLite 音频缓存
│       ├── tts_logger.py       # 日志系统
│       ├── audio_utils.py      # 混音工具库
│       └── app_config.py       # 环境检测
├── resources/
│   ├── configs/
│   │   ├── workflow_config.json
│   │   ├── tts_audio_cache.db
│   │   └── ui.ico
│   ├── tts_audio_cache/        # TTS 音频缓存
│   ├── batch_tts_output/       # 批量输出
│   └── logs/                   # TTS 运行日志
└── workspace/<字幕MD5>/        # 生成的配音片段（扁平结构）
```

## 音频缓存

TTS 合成结果基于 **文本内容 + 声音模型** 进行 MD5 去重缓存（SQLite + 本地音频文件），相同字幕内容不会重复合成。缓存位于 `resources/tts_audio_cache/`。

可通过 **TTS 配置 → 清除缓存** 按钮一键清理，或手动删除 `resources/tts_audio_cache/` 目录和 `resources/configs/tts_audio_cache.db`。

## 变更记录

### v2.0.2 (2026-08-17)
- **进度条精确化**：TTS 进度统一为 0-100 百分比，批量/流水线/文件夹组模式下恢复"任务内片段进度 + 任务间累计"的平滑混合显示，消除"长时间不动突然跳"、clamp 假 100% 和回退横跳
- **日志显示优化**：批量/流水线模式下 worker 日志统一加任务名前缀（多任务并发可区分归属）；日志行加时间戳，并按结果着色（成功绿 / 失败红 / 警告橙）；日志面板上限 5000 行防无限增长
- **流水线日志补全**：新增"开始字幕提取/语音生成/混音"、"语音阶段失败，跳过混音"（替代静默跳过）、阶段完成等关键事件日志

### v2.0.1 (2026-08-11)
- **修复进度条"第一个任务完成直接 100%"**（两处根因）：任务完成后界面刷新不再覆盖累计进度（批量执行中进度条完全由累计信号驱动）；多任务批量不再把 TTS 片段总数误用作进度条上限（百分比被截断成 100% 的问题）
- **修复混音等待/干活期间进度条一直 0%**：流水线模式转发混音、字幕的任务内进度（映射到任务累计区间），提取音轨、加载配音、声道检测、合成、导出阶段进度实时可见
- 批量/组执行启动时重置三个步骤进度条，避免残留上次运行的值

### v2.0.0 (2026-08-09)
- **新增流水线批量执行模式**：字幕先全部完成，语音生成与混音交错并行——音频语音完成立即混音，同时继续生成下一个音频的语音，多音频批量总耗时大幅缩短
- 语音并行控制：新增"全局并发上限"（默认 8，防 Edge TTS 限流）和"流水线语音并行"（默认 2，同时生成语音的音频数）
- 混音并行真正生效：流水线模式的混音并行度改为跟随混音配置"线程数"，多任务可同时混音
- 修复多个问题：流水线跨线程信号回调失效（语音报"未执行"）、语音完成后任务列表不刷新、进度条左右横跳、by_task 语音"第一个任务完成就 100%"
- 批量/组执行三个步骤进度条统一为"已完成任务数 / 总任务数"累计递增
- 修复批量后部分文件夹未添加"双语-"前缀（漏掉跳过任务）并支持失败自动重试

### v1.5.0 (2026-08-09)
- 任务列表支持**文件夹分组**：拖入文件夹创建分组节点，展开显示内部音频树，组内音频逐个执行（字幕→配音→混音），右键可移除整组或重跑组内步骤
- 修复选择任务后三个步骤进度条残留上一次运行值的问题，现按字幕/语音/混音各自的完成状态显示（完成=100%、待处理=0%、运行中=实时进度）
- 修复批量/文件夹组混音时进度条"一个完成直接走完"的问题，现按"已完成混音数/总音频数"递增
- 步骤1 单任务字幕提取改为按文件识别（不再把整个文件夹一次性传给 infer.exe）

### v1.4.0 (2026-08-09)
- 混音配置新增"导出音频质量"：档位预设（低/标准/高/极致/自定义）+ 比特率（128k~320k）、采样率（44.1kHz/48kHz）、声道（立体声/单声道）可调，WAV 导出支持 16/24/32bit 位深
- 导出参数从硬编码 192k/44.1kHz/2ch 提升为可配置项，覆盖纯音频导出（pydub/NVENC）与视频换音轨两条路径
- 批量 API 请求增加异常兜底，服务器未返回音频时正确标记失败而非漏报
- 新增 API 服务器连通性测试函数 `test_api_server`

### v1.3.0 (2026-08-03)
- 步骤1 字幕提取支持文件夹模式：文件夹导入的任务直接传文件夹给 infer.exe，自动扫描其中全部音频，避免逐个文件处理；批量模式同样改为传去重后的文件夹列表，显著提升处理速度
- 文件夹模式下字幕查找仅使用精确文件名匹配，避免误关联同目录下其它文件的字幕
- 优化打包体积：spec 排除误收集的无关依赖（Pythonwin、pythonnet、PIL、cryptography、pydantic 等）及未使用的 Qt DLL（Qt6Pdf/Qt6Network/Qt6Svg），打包体积由 93.3MB 降至 64.2MB

### v1.2.2 (2026-07-29)
- 优化启动速度：将步骤模块（whisper/tts/mixer/audio_utils/batch_executor）改为延迟导入，避免启动时加载 numpy、pydub、edge-tts、requests 等重型库

### v1.2.1 (2026-07-29)
- 优化启动速度：ffmpeg NVENC 检测从模块导入时改为懒加载，避免启动时阻塞等待 `ffmpeg -encoders`

### v1.2.0
- TTS 配置面板新增缓存管理：显示缓存大小和复用统计，支持一键清除
- 修复 Edge TTS 合成后临时 .mp3 文件泄漏（每片段浪费双份磁盘空间）
- 修复 `auto_volume` 动态音量模式因字符串不匹配完全失效的问题
- 修复全部流程完成后偶发闪退（重命名函数未处理跨盘符 ValueError）
- 修复 SQLite 连接漏关、全局缓存无限增长、任务状态不持久化等问题
- 优化混音内存占用（64bit→32bit，消除中间副本，内存减半）
- 优化启动速度（合并冗余 subprocess 调用）
- 任务列表百分比移至文件名最前，方便按进度排序
- 清理死代码、配置残留，添加 .gitignore

### v1.1.0
- 任务结构简化：移除 `workspace/<timestamp>/` 中间目录，语音文件直接存于 `workspace/<字幕MD5>/`
- 添加任务时自动检测已生成的语音和混音文件，自动跳过已完成步骤
- 批量执行进度条修复，三个步骤均可实时显示进度
- TTS 缓存与日志路径统一到 `resources/` 目录
- 全局屏蔽 subprocess 控制台窗口弹出
- 应用图标设置
- 新增 PyInstaller 打包配置
- 修复打包后 `sys` 未导入导致的启动崩溃
- Edge TTS 单条生成添加 15 秒超时，防止网络卡住导致进度永久阻塞
- Edge TTS 超时/异常后自动重试（最多 3 次，间隔 1 秒）
- 修复 `subprocess.Popen` 猴子补丁与 `asyncio` 冲突（改用类继承方式）
- 新增文件夹拖放导入（"+ 添加文件夹"按钮，自动递归扫描音频和字幕）
- 新增混音完成后自动为源文件夹添加"双语-"前缀（混音配置中可开关）
- 修复文件夹重命名时机（同文件夹所有任务完成后才重命名，避免混音中断）
- 修复重命名目标层级（重命名拖入的根文件夹而非音频文件父目录）
- 修复文件夹扫描不支持 `.wav`/`.wma` 格式的问题

### v1.0.0
- 初始版本：三步骤工作流整合
