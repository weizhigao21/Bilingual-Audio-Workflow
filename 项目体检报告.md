# 双语音声工作流 · 项目体检报告

> 体检日期：2026-07-28
> 项目版本：v1.1.0
> 体检范围：17 个 Python 源文件、配置文件、打包配置、workspace/resources 运行时状态

---

## 一、问题汇总

| 严重等级 | 数量 | 说明 |
|---------|------|------|
| P0 严重 Bug | 2 | 功能性故障，功能完全不工作 |
| P1 重要问题 | 5 | 资源泄漏、逻辑错误，影响稳定性 |
| P2 性能问题 | 3 | 影响启动速度、内存占用、日志膨胀 |
| P3 代码质量 | 6 | 死代码、配置残留、重复逻辑 |

---

## 二、P0 严重 Bug

### 2.1 auto_volume 动态音量模式完全失效

**文件**：`src/steps/audio_utils.py:606` vs `src/widgets.py:579`

**问题**：配置面板存入的值是 `"auto"`，但混音代码检查的值是 `"dynamic"`，两者永远不匹配。

```python
# widgets.py:579 — 配置面板存入 "auto"
self.auto_vol_combo.addItem("自动匹配原音频响度", "auto")

# audio_utils.py:606 — 代码检查 "dynamic"，永远不匹配
elif auto_volume == "dynamic":
    original_rms_envelope, env_hop_ms = compute_rms_envelope(...)
```

**后果**：用户选择"自动匹配原音频响度"后，实际走的是 else 分支（固定增益 `vol_linear`），动态音量匹配功能从未生效。

**修复方案**：统一字符串。建议把代码改为检查 `"auto"`，或把配置面板的 data 改为 `"dynamic"`。

---

### 2.2 Edge TTS 临时 mp3 文件泄漏

**文件**：`src/steps/tts_utils.py:257-264`

**问题**：Edge TTS 生成 `.mp3` 临时文件后复制到 `.wav` 路径，但从不删除 `.mp3` 原文件。

```python
temp_path = save_path.replace(".wav", ".mp3")  # 生成 .mp3
asyncio.run(asyncio.wait_for(
    _edge_tts_generate(text, voice, rate, volume, temp_path),
    timeout=15
))
audio_cache.save_audio_cache(cache_key, temp_path, ...)
if temp_path != save_path:
    shutil.copy2(temp_path, save_path)  # 复制到 .wav
    # ❌ 缺少 os.unlink(temp_path)
```

**实际影响**：实测 workspace/05de12f7 目录有 262 个文件，其中一半是泄漏的 `.mp3`。26 个任务目录累计浪费约 50% 磁盘空间。

**修复方案**：在 `shutil.copy2` 后添加 `os.unlink(temp_path)`。

---

## 三、P1 重要问题

### 3.1 SQLite 连接从不关闭

**文件**：`src/steps/tts_cache.py:35-38`

**问题**：`AudioCache` 类的 `_get_conn()` 懒加载创建连接，`close()` 方法存在但从未被任何调用方调用。每次创建 `TTSWorker` 都会新建一个 `AudioCache` 实例，产生新的 SQLite 连接，旧连接不关闭。

```python
def _get_conn(self):
    if self._conn is None:
        self._conn = sqlite3.connect(self.db_path, check_same_thread=False)
    return self._conn  # 从不关闭

def close(self):  # 定义了但无人调用
    if self._conn:
        self._conn.close()
        self._conn = None
```

**后果**：多次批量执行后 SQLite 连接累积，可能导致数据库锁竞争或文件句柄耗尽。

**修复方案**：在 `TTSWorker.run()` 的 finally 块中调用 `self.audio_cache.close()`。

---

### 3.2 全局缓存 dict 无限增长

**文件**：`src/steps/audio_utils.py:84, 132`

**问题**：两个模块级字典缓存永不清理：

```python
_mix_files_cache = {}       # 缓存配音文件列表，按目录路径作 key
_voice_channel_cache = {}   # 缓存声道检测结果，按音频文件路径作 key
```

**后果**：长时间运行处理大量任务时，这两个字典持续增长，内存占用线性上升。`_voice_channel_cache` 会缓存每个配音文件的路径，任务越多缓存越大。

**修复方案**：在每个任务完成后调用 `clear_mix_cache()` 和 `clear_voice_cache()`，或改用 `functools.lru_cache` 限制大小。

---

### 3.3 任务状态从不持久化

**文件**：`src/task_manager.py:98-99`

**问题**：`TaskInfo.save()` 是空实现（`pass`），文档注释提到 `task.json` 但从未写入。

```python
def save(self):
    pass  # ❌ 空实现
```

**后果**：程序崩溃或关闭后，所有任务状态（哪些已完成、哪些失败）全部丢失，重启后需要重新添加所有文件。README 提到的"自动检测已存在文件"能部分缓解，但自定义字幕/配音路径等会丢失。

**修复方案**：实现 `save()` 方法将 `to_dict()` 结果写入 `task_json_path`，并在 `TaskQueue.update_task()` 中调用。

---

### 3.4 文件夹重命名路径逻辑反转

**文件**：`src/workflow_gui.py:822-828`

**问题**：`_rename_source_folder` 中的 `commonpath` 判断条件与注释矛盾：

```python
for t in self.task_queue.tasks:
    if os.path.dirname(t.source_path) == new_path:
        continue  # 已更新
    if os.path.commonpath([t.source_path, source_dir]) == source_dir:
        continue  # 注释说"文件不在该目录下（多级子目录）"
                  # 但 commonpath == source_dir 恰恰说明文件在该目录下
    rel = os.path.relpath(t.source_path, source_dir)
    t.source_path = os.path.join(new_path, rel)
```

**后果**：当 `commonpath == source_dir`（文件确实在源目录中）时执行 `continue` 跳过更新，导致该更新的任务路径反而没更新；而不在源目录的文件反而尝试计算 relpath，可能抛出 ValueError。

**修复方案**：修正条件为 `commonpath != source_dir` 时 continue，或直接用 `t.source_path.startswith(source_dir + os.sep)` 判断。

---

### 3.5 rstrip("_mixed") 误用为后缀删除

**文件**：`src/task_manager.py:203`

**问题**：`str.rstrip()` 删除的是字符集合中的任意字符，不是后缀字符串。

```python
stem = name.rstrip("_mixed")
```

**后果**：对于文件名如 `"video_mixed"`，`rstrip` 会反复删除 `_`、`m`、`i`、`x`、`e`、`d` 这些字符，直到遇到不在集合中的字符。如果源文件名恰好以这些字符结尾（如 `"video_mad"`），会被错误截断为 `"vi"`。

**修复方案**：使用 `name.removesuffix("_mixed")`（Python 3.9+）或手动检查 `endswith`。

---

## 四、P2 性能问题

### 4.1 长音频内存爆炸

**文件**：`src/steps/audio_utils.py:555-749`

**问题**：`mix_with_numpy` 对整段音频同时持有 4 份完整副本：

| 数组 | 用途 | 1小时 44.1kHz 16bit 立体声 |
|------|------|---------------------------|
| `stereo` | 原音频转 float64 | ~316 MB |
| `mix_buffer` | 混音叠加缓冲区 | ~316 MB |
| `result = stereo + mix_buffer` | 结果数组 | ~316 MB |
| `mono_samples` | 内容对齐预计算 | ~158 MB |
| **合计** | | **~1.1 GB** |

此外 `detect_angles_parallel` 还会额外生成 `full_samples_2d`（~158 MB float32）。

**后果**：处理 1 小时以上的视频时，内存峰值可达 1.2 GB 以上，32 位 Python 或低配机器可能 OOM。

**修复方案**：
- 用 `float32` 代替 `float64`，内存减半
- `mix_buffer` 用 `np.add.at` 原地累加到 `stereo`，省一份副本
- 内容对齐的 `mono_samples` 用 `stereo.mean(axis=1)` 的 view 或延迟计算

---

### 4.2 启动时 3 次 subprocess 调用

**文件**：`src/steps/app_config.py:62-83`

**问题**：模块导入时立即执行 3 次 subprocess（`check_nvenc_available`、`check_cuda_available`、`check_aac_nvenc_available`），每次都启动子进程读取 stdout：

```python
NVENC_AVAILABLE = check_nvenc_available()      # subprocess: ffmpeg -encoders
CUDA_AVAILABLE = check_cuda_available()        # subprocess: nvidia-smi -L
AAC_NVENC_AVAILABLE = check_aac_nvenc_available()  # subprocess: ffmpeg -encoders (重复!)
```

**后果**：冷启动时额外增加 0.3-1 秒延迟（取决于磁盘和系统负载）。`check_aac_nvenc_available` 与 `check_nvenc_available` 解析的是同一个 ffmpeg 输出，完全可以合并。

**修复方案**：
- 合并为一次 `ffmpeg -encoders` 调用，解析结果同时判断 h264_nvenc 和 aac_nvenc
- 改为延迟检测（首次使用时才检查），避免不影响混音的场景也付出代价

---

### 4.3 日志无轮转 + 调试日志残留

**文件**：`src/steps/tts_logger.py`、`src/workflow_gui.py:771`

**问题 A**：日志文件按天创建但从不清理，实测已有 3 个日志文件共 ~770KB，长期使用会持续增长。

**问题 B**：进度回调中残留调试日志，每次进度更新都向日志区追加一行：
```python
self._append_log(f"[调试] _on_step_progress(step={step}, value={value}, _tts_total={self._tts_total})")
```
批量执行时进度回调频率很高，这些调试日志会刷屏并拖慢 UI（`QTextEdit.append` + 滚动到底部）。

**修复方案**：
- 日志改用 `logging.handlers.RotatingFileHandler`，限制单文件大小和保留数量
- 移除或降级 `[调试]` 日志为 `logger.debug` 级别，不输出到 UI

---

## 五、P3 代码质量

### 5.1 死代码残留

| 文件 | 内容 | 说明 |
|------|------|------|
| `src/external_loader.py` | 整个文件 | 定义了外部项目加载器，但全项目无人调用 |
| `src/steps/audio_utils.py:27-81` | `AudioPreloader` 类 | 定义了音频预加载器，但从未实例化 |
| `src/steps/app_config.py:135-141` | `tkinterdnd2` 导入 | 尝试导入 tkinterdnd2，但项目用 PyQt6 做拖拽，完全无关 |

**建议**：直接删除这些死代码，减少维护负担和打包体积。

---

### 5.2 配置残留不一致

- `workflow_config.json` 中 `subprojects` 还保留 `tts_dir` 和 `mixer_dir`，但代码只用 `whisper_dir`，另外两个是旧版残留
- `app_config.py` 有独立的 `load_config()`/`save_config()` 读写 `mixer_config.json`，但主程序用的是 `WorkflowConfig` 读 `workflow_config.json`，`mixer_config.json` 从未被主程序使用
- 实际配置 `compute_type: "float32"` 比默认 `"auto"` 慢，`edge_threads: 12` 比默认 5 高，可能触发 Edge TTS 限流

**建议**：清理 `subprojects` 中的废弃字段；删除 `app_config.py` 中的 `load_config`/`save_config`；评估 compute_type 和 edge_threads 是否需要调整。

---

### 5.3 .pyc 文件打入仓库

`src/` 下有 17 个 `.pyc` 文件和 3 个 `__pycache__` 目录被提交到仓库。这些是编译缓存，不应版本管理。

**建议**：添加 `.gitignore` 规则 `__pycache__/` 和 `*.pyc`，并从仓库中移除已有文件。

---

### 5.4 API 选择逻辑重复

**文件**：`src/steps/tts_worker.py`

`run()` 方法（286-314 行）和 `_run_api_tts()` 方法（407-432 行）中，"获取可用 API 列表"的逻辑完全重复：

```python
# 两处完全相同的代码
if self.config.get("use_multi_api", False):
    available_apis = [api for api in ... if api.get("status") == "success"]
    if not available_apis:
        ...
else:
    ...
```

**建议**：抽取为 `_get_available_apis()` 私有方法。

---

### 5.5 SQLite 线程安全不完整

**文件**：`src/steps/tts_cache.py`

`get_cache_stats()` 和 `get_cache_details()` 访问数据库时未加锁，而其他方法都用了 `self._lock`。在多线程 TTS 场景下可能读到不一致状态。

**建议**：给这两个方法也加上 `with self._lock:`。

---

### 5.6 主线程阻塞 sleep

**文件**：`src/workflow_gui.py:851`

`_rename_batch_folders` 在主线程（批量完成信号回调）中调用 `time.sleep(0.5)`，会短暂冻结 UI。

**建议**：用 `QTimer.singleShot(500, ...)` 延迟执行，不阻塞事件循环。

---

## 六、优先修复建议

### 立即修复（P0）
1. **auto_volume 字符串统一** — 一行改动，恢复动态音量功能
2. **Edge TTS mp3 清理** — 加一行 `os.unlink(temp_path)`，消除文件泄漏

### 短期修复（P1）
3. **SQLite 连接关闭** — 在 TTSWorker 结束时调用 `close()`
4. **全局缓存清理** — 任务完成后调用 `clear_mix_cache()` / `clear_voice_cache()`
5. **重命名路径逻辑修正** — 反转 commonpath 条件
6. **rstrip 改 removesuffix** — 防止文件名误截断
7. **任务持久化** — 实现 `task.save()` 并在状态变更时调用

### 中期优化（P2）
8. **混音内存优化** — float32 + 原地累加
9. **启动 subprocess 合并** — 3 次合并为 1 次
10. **日志轮转 + 移除调试日志**

### 长期清理（P3）
11. 删除死代码
12. 清理配置残留
13. 添加 .gitignore
14. 抽取重复逻辑

---

## 七、项目亮点

体检中也发现了一些做得好的地方：

- **subprocess 猴子补丁改为类继承**（main.py:16-28）— 正确解决了与 asyncio 的冲突
- **numpy 向量化混音** — `mix_with_numpy` 用向量化操作替代逐样本循环，性能优秀
- **声道检测并行化** — `detect_angles_parallel` 一次性转 numpy 数组后用 ThreadPoolExecutor 并行检测
- **TTS 音频缓存** — SQLite + 文件双缓存，基于文本+模型哈希去重，有效减少重复合成
- **批量执行双模式** — 支持按任务/按步骤两种遍历顺序，步骤1批量加载模型、步骤3并行混音，设计合理
- **防休眠机制** — TTS 执行期间阻止系统休眠，避免网络中断
- **信号驱动架构** — QThread + pyqtSignal + QEventLoop 的事件驱动设计，避免阻塞 UI
